import React from 'react';

import { Container, LoadingText } from './LoadingSpinnerStyles';

const LoadingSpinner = () => (
  <Container>
    <LoadingText className="h3">Loading...</LoadingText>
  </Container>
);

export default LoadingSpinner;
